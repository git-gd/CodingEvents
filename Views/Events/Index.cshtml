<h1>Coding Events</h1>
@if (ViewBag.events.Count == 0)
{
    <p>No events!</p>
}
else
{
    <ul>
        @foreach (string meeting in ViewBag.events)
        {
            <li>@meeting</li>
        }
    </ul>
}
<a href="/Events/Add">Add New Event</a>